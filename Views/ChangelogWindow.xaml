<Window x:Class="ChaturbateRecorderApp.Views.ChangelogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="clr-namespace:ChaturbateRecorderApp.UI"
        Title="{Binding Titre}"
        Width="560" Height="520"
        MinWidth="420" MinHeight="320"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
            <ItemsControl ItemsSource="{Binding Versions}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="0,0,0,16">
                            <TextBlock Text="{Binding Titre}"
                                       FontWeight="SemiBold" FontSize="14"
                                       Foreground="{DynamicResource Brush.Fg}" />
                            <ItemsControl ItemsSource="{Binding Changements}" Margin="0,6,0,0">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="0,3,0,0">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>
                                            <!-- Puce et texte en DEUX colonnes,
                                                 pas un « • » collé devant la
                                                 chaîne : sur un retour à la
                                                 ligne, la suite s'aligne sous le
                                                 texte et non sous la puce. -->
                                            <TextBlock Grid.Column="0" Text="•" Margin="0,0,8,0"
                                                       Foreground="{DynamicResource Brush.FgMuted}" />
                                            <TextBlock Grid.Column="1" Text="{Binding}"
                                                       TextWrapping="Wrap"
                                                       Foreground="{DynamicResource Brush.Fg}" />
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <Button Grid.Row="1" Margin="0,12,0,0"
                HorizontalAlignment="Right"
                Style="{StaticResource Button.Primary}"
                Content="{ui:Str button.close}"
                Click="Fermer_Click" />
    </Grid>
</Window>
