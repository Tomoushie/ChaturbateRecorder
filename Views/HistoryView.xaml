<!-- Views/HistoryView.xaml -->
<UserControl x:Class="ChaturbateRecorderApp.Views.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:models="clr-namespace:ChaturbateRecorderApp.Models"
             xmlns:viewModels="clr-namespace:ChaturbateRecorderApp.ViewModels"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=viewModels:HistoryViewModel}">

    <!-- Plus besoin de déclarer les converters localement -->
    <!-- <UserControl.Resources>
        <converters:FileSizeConverter x:Key="FileSizeConverter" />
        <converters:DateTimeToStringConverter x:Key="DateTimeToStringConverter" />
    </UserControl.Resources> -->

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Barre d'outils -->
            <RowDefinition Height="*"/>   <!-- DataGrid -->
        </Grid.RowDefinitions>

        <!-- Barre d'outils -->
        <Border Grid.Row="0" Background="{DynamicResource MaterialDesignPaper}" CornerRadius="4" Padding="10" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal">
                <Button Content="Rafraîchir"
                        Command="{Binding RefreshCommand}"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Margin="0,0,10,0"/>
                <Button Content="Ouvrir Dossier"
                        Command="{Binding OpenFolderCommand}"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Margin="0,0,10,0"/>
                <!-- Ajoutez d'autres boutons si nécessaire (Filtrer, Supprimer, etc.) -->
                <TextBlock Text="{Binding EntryCount, StringFormat='Entrées : {0}'}" VerticalAlignment="Center" Margin="20,0,0,0"/>
            </StackPanel>
        </Border>

        <!-- DataGrid pour afficher l'historique -->
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding HistoryEntries}"
                  SelectedItem="{Binding SelectedEntry, Mode=TwoWay}" <!-- Lier l'élément sélectionné -->
                  AutoGenerateColumns="False"
                  HeadersVisibility="Column"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  IsReadOnly="True"
                  GridLinesVisibility="All"
                  SelectionMode="Single"
                  materialDesign:DataGridAssist.CellPadding="8,4,8,4"> <!-- Ajuste la marge des cellules MaterialDesign -->

            <DataGrid.Columns>
                <!-- Colonne Miniature -->
                <DataGridTemplateColumn Header="Miniature" Width="SizeToCells" MinWidth="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate DataType="{x:Type models:HistoryItem}">
                            <Image Source="{Binding ThumbnailPath}" Width="80" Height="45" Stretch="Uniform" Margin="2"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!-- Colonne Nom -->
                <DataGridTextColumn Header="Fichier" Binding="{Binding Name}" Width="*"/>

                <!-- Colonne Taille -->
                <DataGridTextColumn Header="Taille" Binding="{Binding Size, Converter={StaticResource FileSizeConverter}}" Width="80"/>

                <!-- Colonne Durée -->
                <DataGridTextColumn Header="Durée" Binding="{Binding Duration}" Width="80"/>

                <!-- Colonne Date -->
                <DataGridTextColumn Header="Date" Binding="{Binding Date, Converter={StaticResource DateTimeToStringConverter}}" Width="120"/>

                <!-- Colonne Plateforme -->
                <DataGridTextColumn Header="Plateforme" Binding="{Binding PlatformBadge}" Width="80"/>

                <!-- Colonne Actions (Optionnelle) -->
                <DataGridTemplateColumn Header="Actions" Width="SizeToCells" MinWidth="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate DataType="{x:Type models:HistoryItem}">
                            <Button Content="Ouvrir"
                                    Command="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=DataContext.OpenFileCommand}"
                                    CommandParameter="{Binding .}" <!-- Passe l'HistoryItem courant -->
                                    Style="{StaticResource MaterialDesignFlatButton}"
                                    Margin="2"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>