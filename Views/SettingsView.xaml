<UserControl x:Class="ChaturbateRecorderApp.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:ChaturbateRecorderApp.ViewModels"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=viewModels:SettingsViewModel}">

    <ScrollViewer Margin="10">
        <StackPanel Orientation="Vertical" Spacing="10">

            <!-- Section Dossiers -->
            <GroupBox Header="Dossiers" Margin="0,0,0,10" Style="{StaticResource MaterialDesignCardGroupBox}">
                <StackPanel Orientation="Vertical" Spacing="5">
                    <materialDesign:Card Margin="0,5">
                        <Grid Margin="10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="Label"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Dossier de capture :" VerticalAlignment="Center" Grid.Column="0" Margin="0,0,10,0"/>
                            <TextBox Text="{Binding CaptureDir, UpdateSourceTrigger=PropertyChanged}" Grid.Column="1" Margin="0,0,10,0"/>
                            <Button Command="{Binding BrowseCaptureDirCommand}" Content="..." Grid.Column="2" Width="30" Style="{StaticResource MaterialDesignToolButton}"/>
                        </Grid>
                    </materialDesign:Card>

                     <materialDesign:Card Margin="0,5">
                        <Grid Margin="10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="Label"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Dossier des logs :" VerticalAlignment="Center" Grid.Column="0" Margin="0,0,10,0"/>
                            <TextBox Text="{Binding LogDir, UpdateSourceTrigger=PropertyChanged}" Grid.Column="1" Margin="0,0,10,0"/>
                            <Button Command="{Binding BrowseLogDirCommand}" Content="..." Grid.Column="2" Width="30" Style="{StaticResource MaterialDesignToolButton}"/>
                        </Grid>
                    </materialDesign:Card>

                    <materialDesign:Card Margin="0,5">
                        <Grid Margin="10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="Label"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Dossier des outils :" VerticalAlignment="Center" Grid.Column="0" Margin="0,0,10,0"/>
                            <TextBox Text="{Binding ToolsDir, UpdateSourceTrigger=PropertyChanged}" Grid.Column="1" Margin="0,0,10,0"/>
                            <Button Command="{Binding BrowseToolsDirCommand}" Content="..." Grid.Column="2" Width="30" Style="{StaticResource MaterialDesignToolButton}"/>
                        </Grid>
                    </materialDesign:Card>

                     <materialDesign:Card Margin="0,5">
                        <Grid Margin="10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="Label"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Dossier temporaire :" VerticalAlignment="Center" Grid.Column="0" Margin="0,0,10,0"/>
                            <TextBox Text="{Binding TempDir, UpdateSourceTrigger=PropertyChanged}" Grid.Column="1" Margin="0,0,10,0"/>
                            <Button Command="{Binding BrowseTempDirCommand}" Content="..." Grid.Column="2" Width="30" Style="{StaticResource MaterialDesignToolButton}"/>
                        </Grid>
                    </materialDesign:Card>

                     <materialDesign:Card Margin="0,5">
                        <Grid Margin="10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="Label"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Dossier des miniatures :" VerticalAlignment="Center" Grid.Column="0" Margin="0,0,10,0"/>
                            <TextBox Text="{Binding ThumbnailsDir, UpdateSourceTrigger=PropertyChanged}" Grid.Column="1" Margin="0,0,10,0"/>
                            <Button Command="{Binding BrowseThumbnailsDirCommand}" Content="..." Grid.Column="2" Width="30" Style="{StaticResource MaterialDesignToolButton}"/>
                        </Grid>
                    </materialDesign:Card>
                </StackPanel>
            </GroupBox>

            <!-- Section Chemins des binaires -->
            <GroupBox Header="Chemins des binaires" Margin="0,0,0,10" Style="{StaticResource MaterialDesignCardGroupBox}">
                <StackPanel Orientation="Vertical" Spacing="5">
                    <materialDesign:Card Margin="0,5">
                        <Grid Margin="10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="Label"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Chemin de yt-dlp :" VerticalAlignment="Center" Grid.Column="0" Margin="0,0,10,0"/>
                            <TextBox Text="{Binding YtDlpPath, UpdateSourceTrigger=PropertyChanged}" Grid.Column="1" Margin="0,0,10,0"/>
                            <Button Command="{Binding BrowseYtDlpPathCommand}" Content="..." Grid.Column="2" Width="30" Style="{StaticResource MaterialDesignToolButton}"/>
                        </Grid>
                    </materialDesign:Card>

                    <materialDesign:Card Margin="0,5">
                        <Grid Margin="10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="Label"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Chemin de FFmpeg :" VerticalAlignment="Center" Grid.Column="0" Margin="0,0,10,0"/>
                            <TextBox Text="{Binding FfmpegPath, UpdateSourceTrigger=PropertyChanged}" Grid.Column="1" Margin="0,0,10,0"/>
                            <Button Command="{Binding BrowseFfmpegPathCommand}" Content="..." Grid.Column="2" Width="30" Style="{StaticResource MaterialDesignToolButton}"/>
                        </Grid>
                    </materialDesign:Card>

                    <materialDesign:Card Margin="0,5">
                        <Grid Margin="10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="Label"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Chemin de FFprobe :" VerticalAlignment="Center" Grid.Column="0" Margin="0,0,10,0"/>
                            <TextBox Text="{Binding FfprobePath, UpdateSourceTrigger=PropertyChanged}" Grid.Column="1" Margin="0,0,10,0"/>
                            <Button Command="{Binding BrowseFfprobePathCommand}" Content="..." Grid.Column="2" Width="30" Style="{StaticResource MaterialDesignToolButton}"/>
                        </Grid>
                    </materialDesign:Card>
                </StackPanel>
            </GroupBox>

            <!-- Section Connexion -->
            <GroupBox Header="Connexion" Margin="0,0,0,10" Style="{StaticResource MaterialDesignCardGroupBox}">
                <StackPanel Orientation="Vertical" Spacing="5">
                    <materialDesign:Card Margin="0,5">
                        <Grid Margin="10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="Label"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Fichier de cookies :" VerticalAlignment="Center" Grid.Column="0" Margin="0,0,10,0"/>
                            <TextBox Text="{Binding CookiesFilePath, UpdateSourceTrigger=PropertyChanged}" Grid.Column="1" Margin="0,0,10,0"/>
                            <Button Command="{Binding BrowseCookiesPathCommand}" Content="..." Grid.Column="2" Width="30" Style="{StaticResource MaterialDesignToolButton}"/>
                        </Grid>
                    </materialDesign:Card>

                    <materialDesign:Card Margin="0,5">
                        <Grid Margin="10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="Label"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="URL du proxy :" VerticalAlignment="Center" Grid.Column="0" Margin="0,0,10,0"/>
                            <TextBox Text="{Binding ProxyUrl, UpdateSourceTrigger=PropertyChanged}" Grid.Column="1"/>
                        </Grid>
                    </materialDesign:Card>
                </StackPanel>
            </GroupBox>

            <!-- Section Surveillance -->
            <GroupBox Header="Surveillance" Margin="0,0,0,10" Style="{StaticResource MaterialDesignCardGroupBox}">
                <StackPanel Orientation="Vertical" Spacing="5">
                    <materialDesign:Card Margin="0,5">
                        <Grid Margin="10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="Label"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Intervalle de vérification (s) :" VerticalAlignment="Center" Grid.Column="0" Margin="0,0,10,0"/>
                            <TextBox Text="{Binding WatchIntervalSeconds, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" Grid.Column="1"/>
                        </Grid>
                    </materialDesign:Card>

                    <materialDesign:Card Margin="0,5">
                        <Grid Margin="10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="Label"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <CheckBox Content="Reconnexion automatique par défaut" IsChecked="{Binding AutoReconnectDefault}" VerticalAlignment="Center" Grid.Column="0" Grid.ColumnSpan="2" HorizontalAlignment="Left" Margin="0,0,0,0"/>
                        </Grid>
                    </materialDesign:Card>
                </StackPanel>
            </GroupBox>

            <!-- Section Enregistrement -->
            <GroupBox Header="Enregistrement" Margin="0,0,0,10" Style="{StaticResource MaterialDesignCardGroupBox}">
                <StackPanel Orientation="Vertical" Spacing="5">
                    <materialDesign:Card Margin="0,5">
                        <Grid Margin="10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="Label"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Format par défaut (yt-dlp) :" VerticalAlignment="Center" Grid.Column="0" Margin="0,0,10,0"/>
                            <TextBox Text="{Binding DefaultFormat, UpdateSourceTrigger=PropertyChanged}" Grid.Column="1"/>
                        </Grid>
                    </materialDesign:Card>

                    <materialDesign:Card Margin="0,5">
                        <Grid Margin="10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="Label"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Conteneur par défaut :" VerticalAlignment="Center" Grid.Column="0" Margin="0,0,10,0"/>
                            <ComboBox SelectedItem="{Binding DefaultContainer, UpdateSourceTrigger=PropertyChanged}" ItemsSource="{Binding Source={x:Static viewModels:SettingsViewModel.AvailableContainers}}" Grid.Column="1"/>
                        </Grid>
                    </materialDesign:Card>
                </StackPanel>
            </GroupBox>

            <!-- Section Interface -->
            <GroupBox Header="Interface" Margin="0,0,0,10" Style="{StaticResource MaterialDesignCardGroupBox}">
                <StackPanel Orientation="Vertical" Spacing="5">
                    <materialDesign:Card Margin="0,5">
                        <Grid Margin="10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="Label"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Thème :" VerticalAlignment="Center" Grid.Column="0" Margin="0,0,10,0"/>
                            <ComboBox SelectedItem="{Binding Theme, UpdateSourceTrigger=PropertyChanged}" ItemsSource="{Binding Source={x:Static viewModels:SettingsViewModel.AvailableThemes}}" Grid.Column="1"/>
                        </Grid>
                    </materialDesign:Card>

                    <materialDesign:Card Margin="0,5">
                        <Grid Margin="10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="Label"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Langue :" VerticalAlignment="Center" Grid.Column="0" Margin="0,0,10,0"/>
                            <ComboBox SelectedItem="{Binding Language, UpdateSourceTrigger=PropertyChanged}" ItemsSource="{Binding Source={x:Static viewModels:SettingsViewModel.AvailableLanguages}}" Grid.Column="1"/>
                        </Grid>
                    </materialDesign:Card>

                     <materialDesign:Card Margin="0,5">
                        <Grid Margin="10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="Label"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <CheckBox Content="Afficher le panneau des logs au démarrage" IsChecked="{Binding ShowLogs}" VerticalAlignment="Center" Grid.Column="0" Grid.ColumnSpan="2" HorizontalAlignment="Left" Margin="0,0,0,0"/>
                        </Grid>
                    </materialDesign:Card>
                </StackPanel>
            </GroupBox>

            <!-- Section Divers -->
            <GroupBox Header="Divers" Margin="0,0,0,10" Style="{StaticResource MaterialDesignCardGroupBox}">
                <StackPanel Orientation="Vertical" Spacing="5">
                    <materialDesign:Card Margin="0,5">
                        <Grid Margin="10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="Label"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Taille max. des logs (octets) :" VerticalAlignment="Center" Grid.Column="0" Margin="0,0,10,0"/>
                            <TextBox Text="{Binding LogMaxFileSizeBytes, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" Grid.Column="1"/>
                        </Grid>
                    </materialDesign:Card>

                     <materialDesign:Card Margin="0,5">
                        <Grid Margin="10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="Label"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Timeout Watchdog yt-dlp (s) :" VerticalAlignment="Center" Grid.Column="0" Margin="0,0,10,0"/>
                            <TextBox Text="{Binding YtDlpWatchdogTimeoutSeconds, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" Grid.Column="1"/>
                        </Grid>
                    </materialDesign:Card>
                </StackPanel>
            </GroupBox>

            <!-- Boutons Sauvegarder/Annuler -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="10" Margin="0,20,0,0">
                <Button Content="Annuler" Command="{Binding CancelChangesCommand}" Style="{StaticResource MaterialDesignFlatButton}" Margin="0,0,10,0"/>
                <Button Content="Sauvegarder" Command="{Binding SaveSettingsCommand}" Style="{StaticResource MaterialDesignRaisedButton}" Background="{DynamicResource PrimaryHueMidBrush}" Foreground="{DynamicResource PrimaryHueMidForegroundBrush}"/>
            </StackPanel>

        </StackPanel>
    </ScrollViewer>
</UserControl>